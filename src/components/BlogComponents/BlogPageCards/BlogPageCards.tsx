import BlogCards from "../../../features/BlogCards";

import styles from "./BlogPageCards.module.css";

export const BlogPageCards = () => {
  return (
    <div className={styles.blogCards}>
      <BlogCards />
    </div>
  );
};
